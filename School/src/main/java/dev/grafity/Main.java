package dev.grafity;

import java.util.ArrayList;
import java.util.Scanner;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        /*Scanner sc  = new Scanner(System.in);
        int choice = -1;
        int classRoomNo = 0;
        Student stdObj = null;
        School school = new School();
        school.name = "Grafity Systems";
        school.roomOne.setName("Room One");
        school.roomOne.setMaxNumberOfStudents(3);
        school.roomTwo.setName("Room Two");
        school.roomTwo.setMaxNumberOfStudents(2);

        while(choice!=0){
            System.out.println("Enter your choice 0) Exit 1) Add Student 2) Remove Student");
            choice = Integer.parseInt(sc.nextLine());
            if(choice == 1){
                stdObj = new Student();
                System.out.println("Enter student id:");
                stdObj.id = Integer.parseInt(sc.nextLine());
                System.out.println("Enter student name:");
                stdObj.name = sc.nextLine();
                System.out.println("Enter student age:");
                stdObj.age = Integer.parseInt(sc.nextLine());
                System.out.println("Enter the classroom");
                classRoomNo = Integer.parseInt(sc.nextLine());
                if(classRoomNo == 1){
                    if(school.roomOne.addStudent(stdObj)){
                        System.out.println("Student successfully added");
                    }else{
                        System.out.println("Class room is already full");
                    }
                }else if(classRoomNo == 2){
                    if(school.roomTwo.addStudent(stdObj)){
                        System.out.println("Student successfully added");
                    }else{
                        System.out.println("Class room is already full");
                    }
                }

            }else if(choice == 2){
                //Remove
            }
        }
        System.out.println(school);

        Student obj = new Student();
        obj.id = 10;*/
    }
}